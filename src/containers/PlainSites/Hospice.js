import React from 'react';
import MajorSiteTemplate from '../../components/MajorSiteTemplate';
import { siteNames } from '../../shared/constants';

const Hospice = () => <MajorSiteTemplate siteSlug={siteNames.HOSPICE} />;

export default Hospice;
